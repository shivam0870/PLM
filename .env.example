# PostgreSQL single env var (preferred)
# DATABASE_URL=postgres://user:password@localhost:5432/fynd_plm

# Or granular variables
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=postgres
# PGDATABASE=fynd_plm

# Server port
PORT=3000
